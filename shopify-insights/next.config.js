// next.config.js (CommonJS) — safe for all Node versions
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // add any experimental flags you need here
  },
  // any other Next config you used previously
};

module.exports = nextConfig;
